local discordia = _G.discordia
local client = _G.client 


return {
    name = 'snow',
    description = 'snow!',
    callback = funbction(message, args)
    local sent_msg = message:reply({
        content = 'Its snowing a lot right now!',
    })
end
}